export interface ClosedStyleOptions{
  stroke:string;
  strokeWidth:number;
  fill:string;
  opacity:number;
}

export interface PolylineStyleOptions{
  stroke:string;
  strokeWidth:number;
  opacity:number;
}
